{
  "name": "quiet-spark",
  "version": "1.0.0",
  "description": "The Quiet Spark - A Story",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p css js && postcss src/css/tailwind.css -o css/styles.css && cp src/index.html ./ && cp src/js/* js/",
    "dev": "mkdir -p css js && postcss src/css/tailwind.css -o css/styles.css --watch",
    "clean": "rm -rf css js index.html"
  },
  "keywords": [],
  "author": "Tommy Caruso",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.24",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.3.2"
  },
  "type": "commonjs"
}
